import React from 'react';

import useStyles from './styles';

const Form = () => {
    // eslint-disable-next-line
    const classes = useStyles();

    return (
        <h1>FORM</h1>
    );
}

export default Form;